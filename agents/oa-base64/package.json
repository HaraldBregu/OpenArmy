{
  "name": "oa-base64",
  "version": "1.0.0",
  "description": "Base64 encoding and decoding agent",
  "bin": {
    "oa-base64": "./bin/cli.js"
  },
  "oa": {
    "description": "Encode or decode base64 text. Use --mode=encode/decode (default: encode)",
    "inputMapping": {
      "type": "flag",
      "flag": "--input"
    }
  },
  "files": ["bin/"],
  "keywords": ["oa", "base64", "encode", "decode"]
}
